#include <iostream>

using namespace std ;

int main() {
int temp 1 , temp 2 , temp 3;

//ptompt user to enter the first three temperature readings 
cout << "Enter first temperature readings: ";
cin >> temp1;

cout<< "Enter second temperature readings (5 minutes later): ";
cin >> temp2;

//check the temperature difference between the first an second readings
int diff = temp2 - temp 1
if (diff >50) {
cout << "increase the fryer heat before taking the third reading." << endl;}

cout << " Enter third reading temperature reading (5 minutes later): ";
cin >> temp3;

//final check to see if the oil is ready for frying
if (temp3 >= 150 & temp3 <= 190) {
count << "oil is not ready for frying!" <<}

return 0;
}
